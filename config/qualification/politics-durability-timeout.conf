# Fixed one-nanosecond deadline used to produce a deterministic detector-policy ERROR outcome.
policies = [
  {
    id = "qualification-request-pii"
    version = "1"
    enabled = true
    match {
      url = "*"
      model = "*"
      phase = "REQUEST"
      subject { type = "*", id = "*" }
    }
    detectors = ["fast-pii"]
    deadline = 1ns
    reactions {
      detected { disposition = "ALLOW", transformations = [] }
      clean { disposition = "ALLOW", transformations = [] }
      error { disposition = "ALLOW", transformations = [] }
    }
    overrides = []
  }
]
